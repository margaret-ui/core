import { ReactNode } from 'react';

export type AvatarProps = {
  imageUrl?: string;
  size?: string;
  shape?: string;
  placeholder?: ReactNode;
  firstName?: string;
  lastName?: string;
  name?: string;
};
